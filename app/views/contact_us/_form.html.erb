<br>
<br>
<div class="row">
<%= form_for(contact_u = ContactU.new) do |f| %>
  <% if contact_u.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(contact_u.errors.count, "error") %> prohibited this contact_u from being saved:</h2>

      <ul>
      <% contact_u.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="input-field col s12 m6 offset-m3">
    <%= f.label (:email) %>
    <%= f.text_field :email, class: "validate" %>
  </div>
   <div class="input-field col s12 m6 offset-m3">
    <%= f.label (:message) %>
    <%= f.text_area :message, id: "textarea1", class: "materialize-textarea" %>
  </div>
  <div class="col m6 offset-m3 waves-effect waves-light btn">
    <%= f.submit t(:send)%>
  </div>  

<% end %>

</div>
